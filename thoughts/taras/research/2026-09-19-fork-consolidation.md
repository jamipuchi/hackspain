# Fork consolidation

Taras requested one clean `main` containing all completed work before parallel implementation resumes.
The consolidation preserves branch history. It does not activate deferred rendering, learning, providers, or adaptive routing.

## Included work

| Delivery | Preserved tip |
|---|---|
| Coffee live engine, restart, and continuous-operation plan | `8c157fd` |
| Coffee quality and custom-object research | `0393583` |
| Coffee assets, moodboard, recording, and cinematic scene | `2fa9853` |
| Jev contracts and magnet adaptive-routing experiment | `edb9270` |

These four tips contain every previously named local branch and every branch on Taras's fork.
The ancestry audit checked all 20 original local and origin references before cleanup.
The older Jev, moodboard, realistic-assets, sorter, closed-loop, and web branches require no separate implementation merge.

## Conflict resolutions

The current coffee roadmap replaces the older roadmap from the render branch.
It already records all chosen render looks and the required later 3D integration.
The magnet controller retains rectified top-down images, Jev imports, OpenRouter accounting, and existing action guards.
Adaptive routing remains opt-in. Historical negative results remain available.

The recording test previously compared historical provenance with newer cinematic scripts.
It now checks immutable script bytes from `a230f2c`, whose parent matches the manifest's recorded source revision.
The movie, poster, and manifest remain unchanged.

## Preserved local work

The local Theker brainstorm is now tracked under `thoughts/taras/brainstorms/`.
The older local coffee plan remains under `thoughts/taras/archive/` as historical material.
The active coffee plans remain under `thoughts/taras/plans/`.
The root ignore file retains credential protection and excludes the local Python environment and generated Python caches.
It no longer hides all new planning documents.

Private backup: `/private/tmp/hackspain-consolidation-20260919-181805`.
It preserves original branch references, the worktree inventory, local credentials, the untracked ignore file, and local thought documents.
Credentials never enter Git. Existing run directories outside retired worktrees remain intact.
Cleanup removes branches only after checking that their tips are ancestors of the published `main`.
No history rewrite, garbage collection, or object pruning belongs to this cleanup.

## Verification

- All 20 original references are ancestors of the consolidation.
- Coffee production source and the live interface match `8c157fd` exactly.
- Python syntax passed for 48 coffee, render, and integration files.
- JavaScript syntax passed for the live interface.
- Three asset delivery checks passed without rendering new artifacts.
- Thirty-six focused magnet checks passed under each of the default and THEKER builds.
- Those magnet checks used mocks. They made no provider calls and started no rendering.

The quality acceptance failure remains unchanged. Capture passed, while good loss and engine speed still missed their requirements.
Taras retains functional QA and acceptance.

## Published baseline

Revision `05763c1` is published to main. All four PRs within Taras's fork now show merged.
The main checkout is clean and uses the exact selected model. Frozen evaluation input validation and model reuse both pass.
The demonstration service moved to the main checkout on port 8890. The retired quality service on port 8891 stopped.
All eight obsolete fork branches without open upstream PRs were removed after ancestry verification.
Taras requested preserving two remote branches because they support open PRs in `jamipuchi/hackspain`.
They are `codex/jev` and `codex/coffee-sorter-upstream`. Neither contains work absent from main.
All former local feature branches were removed.

The user-created detached worktree at `/Users/taras/.codex/worktrees/0833/hackspain` remains intact.
The old core worktree remains detached while an existing file-review window references its plan.
Its branch was removed. Preserve any review comments before removing that final temporary checkout.
Private backups also preserve both completed tasks' local model files.

## Next work

Start new isolated worktrees from the consolidated `main`.
Use Sol for engine and lifecycle changes, Terra for the interface, and Luna for bounded mechanical work.
Do not dispatch Astra agents for this work.
Keep the same 60-second simulation window and explicit settling rules from the approved continuous-operation plan.
Commit and push small increments. Preserve a visible feedback checkpoint before later 3D or public deployment work.
